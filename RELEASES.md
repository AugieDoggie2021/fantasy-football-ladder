## Release & Hotfix Strategy
- Branching: main (prod), develop (integration), feature/*, hotfix/*
- Versioning: SemVer; auto-increment build numbers
- Hotfix: branch from main → fix → bump patch → fastlane `beta` then `release`




